steps:
- name: ubuntu
  args: ['bash', '-c', "echo '$COMMIT_SHA' > REVISION.txt"]
- name: 'gcr.io/cloud-builders/gsutil'
  args:
    - '-m'
    - 'rsync'
    - '-d'
    - '-r'
    - 'dags'
    - 'gs://${_GCS_BUCKET_1}./dags'
/*- name: 'gcr.io/cloud-builders/gsutil'
  args:
    - '-m'
    - 'rsync gs://demo12trial/'
    - '-d'
    - '-r'
    - 'gs://${_GCS_BUCKET_1}./' */
    #Logging
options:
  logging: CLOUD_LOGGING_ONLY
#completed
